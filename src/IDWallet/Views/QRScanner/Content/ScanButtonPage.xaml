<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="IDWallet.Views.QRScanner.Content.ScanButtonPage"
             xmlns:lang="clr-namespace:IDWallet.Extensions"
             BackgroundColor="{StaticResource BackgroundColor}">
    <Button x:Name="RestartScan"
            Clicked="RestartScan_Clicked" Text="{lang:L10n Start_Scan}" TextColor="{StaticResource PrimaryTextColor}"
            BackgroundColor="{StaticResource PrimaryButtonColor}" VerticalOptions="Center" HorizontalOptions="Center"
            WidthRequest="200" HeightRequest="75" />
</ContentPage>